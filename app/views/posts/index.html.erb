<table>

	<tr>
		<th>Title</th>
		<th>Text</th>
	</tr>

	<%= @posts.each do |p| %>
		<tr>
			<td><%= p.title %></td>
			<td><%= p.text %></td>
			<td><%= link_to 'Show Post', post_path(p) %></td>
		</tr>
	<% end %>

	

</table>
